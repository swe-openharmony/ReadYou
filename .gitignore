# HarmonyOS / DevEco build artifacts
oh_modules/
entry/oh_modules/
**/oh_modules/
build/
entry/build/
.hvigor/
entry/.hvigor/
.preview/
entry/.preview/
.test/
entry/.test/
.cxx/

# Per-machine config (each developer regenerates)
local.properties
.idea/
*.iml

# Lockfile policy: keep oh-package-lock.json5 so ohpm install is reproducible
# (don't ignore it)

# DevEco caches
.code-linter/

# Build outputs
*.hap
*.har
*.hsp

# Sign certs (machine-specific)
*.p12
*.cer
*.csr

# OS
Thumbs.db
.DS_Store

# Visual-audit scratch dirs (regenerated by tools/pipeline/_phase_c_capture.py)
_ui_align_tmp/

# Legacy single-shell helpers (replaced by tools/build/_b_build_<App>.bat at repo root)
_build.bat
_ohpm_install.bat
build.bat
_eval/
_eval_page_name_overrides.json
_impl_report.md
phase5_artefacts/
